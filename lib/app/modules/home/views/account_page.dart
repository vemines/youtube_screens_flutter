part of "home_view.dart";

class AccountPage extends GetView<HomeController> {
  const AccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("AccountPage"));
  }
}
